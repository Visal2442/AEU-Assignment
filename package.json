{
  "scripts": {
    "sass": "sass --watch sass:public/css",
    "serve": "lite-server",
    "start": "npm-run-all --parallel sass serve"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "lite-server": "^2.6.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.83.1"
  }
}
